[project]
name = "suma-numeros"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jhon1773", email = "dacher08@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
suma-numeros = "suma_numeros:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
